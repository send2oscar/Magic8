import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { trpc } from "@/lib/trpc";
import { CircleAlert, GalleryHorizontalEnd, LoaderCircle, LogOut, ShieldCheck, UserRound, Users } from "lucide-react";
import { useEffect, useState } from "react";
import { useLocation } from "wouter";

function when(value: Date | string | null | undefined) { if (!value) return "Never"; const date = new Date(value); return Number.isNaN(date.getTime()) ? "Unknown" : date.toLocaleString(); }
function AdminImage({ src, alt }: { src: string | null; alt: string }) { const [failed, setFailed] = useState(false); return !src || failed ? <div className="flex aspect-[4/3] items-center justify-center rounded border border-border bg-muted/30 px-3 text-center text-xs text-muted-foreground">Image unavailable</div> : <img src={src} alt={alt} className="aspect-[4/3] w-full rounded object-cover" onError={() => setFailed(true)} />; }

export default function AdminPanel() {
  const [, setLocation] = useLocation(); const utils = trpc.useUtils(); const session = trpc.admin.session.useQuery(undefined, { retry: false, refetchOnWindowFocus: false }); const authorized = session.data?.authenticated === true; const users = trpc.admin.listUsers.useQuery(undefined, { enabled: authorized, retry: false }); const [selectedUserId, setSelectedUserId] = useState<number | null>(null); const profile = trpc.admin.userProfile.useQuery({ userId: selectedUserId ?? 0 }, { enabled: authorized && selectedUserId !== null, retry: false }); const gallery = trpc.admin.userGallery.useQuery({ userId: selectedUserId ?? 0 }, { enabled: authorized && selectedUserId !== null, retry: false }); const logout = trpc.admin.logout.useMutation({ onSuccess: async () => { await utils.admin.session.invalidate(); setLocation("/admin/login"); } });
  useEffect(() => { if (!session.isLoading && !authorized) setLocation("/admin/login"); }, [authorized, session.isLoading, setLocation]);
  useEffect(() => { if (selectedUserId === null && users.data?.[0]) setSelectedUserId(users.data[0].id); }, [selectedUserId, users.data]);
  if (session.isLoading || !authorized) return <div className="flex min-h-screen items-center justify-center bg-background"><LoaderCircle className="h-8 w-8 animate-spin text-accent" /></div>;
  return <div className="min-h-screen bg-background"><header className="sticky top-0 z-40 border-b-2 border-accent/30 bg-background/85 backdrop-blur"><div className="container flex items-center justify-between gap-4 py-4"><div className="flex items-center gap-3"><ShieldCheck className="h-7 w-7 text-accent" /><div><p className="text-2xl font-bold neon-pink">ADMIN WORKSPACE</p><p className="text-xs text-muted-foreground">Restricted user and gallery review</p></div></div><Button onClick={() => logout.mutate()} disabled={logout.isPending} className="bg-destructive text-destructive-foreground font-bold"><LogOut className="mr-2 h-4 w-4" /> LOGOUT</Button></div></header><main className="container grid gap-6 py-8 lg:grid-cols-[340px_minmax(0,1fr)]"><Card className="hud-frame h-fit bg-card/50 p-4"><div className="mb-4 flex items-center gap-2"><Users className="h-5 w-5 text-accent" /><h1 className="font-bold">USERS</h1></div>{users.isLoading ? <div className="flex justify-center p-8"><LoaderCircle className="h-6 w-6 animate-spin text-accent" /></div> : users.isError ? <p className="text-sm text-destructive">Unable to load users.</p> : users.data?.length ? <div className="max-h-[65vh] space-y-2 overflow-y-auto pr-1">{users.data.map(user => <button key={user.id} onClick={() => setSelectedUserId(user.id)} className={`w-full rounded border p-3 text-left ${selectedUserId === user.id ? "border-secondary bg-secondary/15" : "border-border hover:border-accent/60"}`}><p className="truncate font-semibold">{user.name || "Unnamed user"}</p><p className="truncate text-xs text-muted-foreground">{user.email || "No email recorded"}</p><p className="mt-2 text-xs text-muted-foreground">Last sign-in: {when(user.lastSignedIn)}</p></button>)}</div> : <p className="text-sm text-muted-foreground">No users have signed in yet.</p>}</Card><section className="min-w-0 space-y-6">{selectedUserId === null ? <Card className="hud-frame bg-card/50 p-8 text-center text-muted-foreground">Select a user to review their profile and processing gallery.</Card> : <><Card className="hud-frame bg-card/50 p-6">{profile.isLoading ? <LoaderCircle className="h-6 w-6 animate-spin text-accent" /> : profile.data ? <div className="flex flex-wrap items-start justify-between gap-5"><div className="flex items-start gap-4"><UserRound className="mt-1 h-7 w-7 text-accent" /><div><h2 className="text-2xl font-bold neon-cyan">{profile.data.name || "Unnamed user"}</h2><p className="mt-1 text-muted-foreground">{profile.data.email || "No email recorded"}</p></div></div><dl className="grid grid-cols-2 gap-x-6 gap-y-3 text-sm"><div><dt className="text-muted-foreground">Role</dt><dd className="font-bold uppercase">{profile.data.role}</dd></div><div><dt className="text-muted-foreground">Credits</dt><dd className="font-bold text-secondary">{profile.data.credits}</dd></div><div><dt className="text-muted-foreground">Created</dt><dd>{when(profile.data.createdAt)}</dd></div><div><dt className="text-muted-foreground">Last signed in</dt><dd>{when(profile.data.lastSignedIn)}</dd></div></dl></div> : <p className="text-destructive">This user profile could not be found.</p>}</Card><Card className="hud-frame bg-card/50 p-6"><div className="mb-5 flex items-center gap-2"><GalleryHorizontalEnd className="h-5 w-5 text-accent" /><h2 className="font-bold">USER PROCESSING GALLERY</h2></div>{gallery.isLoading ? <div className="flex justify-center p-8"><LoaderCircle className="h-6 w-6 animate-spin text-accent" /></div> : gallery.isError ? <p className="flex items-center gap-2 text-sm text-destructive"><CircleAlert className="h-4 w-4" /> Unable to load this user&apos;s gallery.</p> : gallery.data?.length ? <div className="grid gap-5 xl:grid-cols-2">{gallery.data.map(entry => <article key={entry.id} className="overflow-hidden rounded border border-accent/20"><div className="flex items-center justify-between border-b border-accent/20 px-4 py-3"><div><p className="font-bold">{entry.shirtStyle}</p><p className="text-xs text-muted-foreground">{when(entry.createdAt)}</p></div><span className="text-xs font-bold uppercase text-accent">{entry.status}</span></div><div className="grid gap-3 p-4 sm:grid-cols-2"><div><p className="mb-2 text-xs text-muted-foreground">UPLOADED</p><AdminImage src={entry.sourceImageUrl} alt="User upload" /></div><div><p className="mb-2 text-xs text-muted-foreground">GENERATED</p><AdminImage src={entry.resultImageUrl} alt="Generated try-on" /></div></div></article>)}</div> : <p className="text-sm text-muted-foreground">This user does not have processing records yet.</p>}</Card></>}</section></main></div>;
}
